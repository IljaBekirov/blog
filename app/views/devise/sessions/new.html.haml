.devise-bs
  %h2 Log in

  .form-horizontal
    = simple_form_for(resource, as: resource_name, url: session_path(resource_name), html: { class: 'form-horizontal' }, defaults: { input_html: { class: 'form-control' } }) do |f|
      .form-inputs
        = f.input :email, required: false, autofocus: true
        = f.input :password, required: false
        = f.input :remember_me, as: :boolean if devise_mapping.rememberable?
      .form-group
        = f.button :submit, "Log in", class: 'btn btn-primary btn-auth'
= render "devise/shared/links"

-#:javascript
-#  $('#change_service_id').change(function() {
-#    var service = $('#change_service_id').val();
-#    $.ajax({
-#      url: "#{services_path + '.json'}" + '?service=' + service,
-#      type: 'PUT'
-#    });
-#  });